__version__ = "0.1.0"

# ICD ICD-COMP-001: CLI ICD / JSON schema / adapter formats are versioned
# independently from the tool release. This module only carries the tool
# release version; see envelope.py for the JSON schema version.
